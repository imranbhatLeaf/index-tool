MONGO_URI=mongodb://localhost:27017/indexer
PORT=5000
INDEXNOW_HOST=example.com
INDEXNOW_KEY=demo-key-replace-me
# Set to true if you don't have a real domain to verify a key against —
# this simulates IndexNow responses so the full pipeline still demos correctly.
INDEXNOW_MOCK=true
